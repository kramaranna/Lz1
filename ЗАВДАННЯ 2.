#include <iostream>
#include <cmath>
using namespace std;

int main() 
{
    double x, y;
    cout<<"Введіть x: ";
    cin>>x;
    y = (2*cos(x)*cos(x))/(1+x*cos(2*x))+pow(0.3,x)/(x*log(x)-2*sin(x)*sin(x));

    cout << "y = " << y<< endl;
    return 0;
}
